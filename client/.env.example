# Public, compiled into the bundle. Development and production use the same paths.
VITE_BACKEND_HTTP_URL=/api/ai
VITE_AUTH_BACKEND_HTTP_URL=/api/auth
# Leave empty to derive WS/WSS and host from the browser location.
VITE_BACKEND_WS_URL=
