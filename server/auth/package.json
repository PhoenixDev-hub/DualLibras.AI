{
  "name": "festival-auth-server",
  "version": "0.1.0",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "test": "npm run build && node --test tests/*.test.cjs",
    "predev": "prisma generate",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "prebuild": "prisma generate",
    "build": "tsc && tsc -p tsconfig.config.json",
    "start": "node dist/server.js",
    "seed:test-users": "npm run build && node dist/scripts/seed-test-users.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "database:admin-accounts": "npm run build && node dist/scripts/update-admin-accounts.js",
    "database:material-classroom": "npm run build && node dist/scripts/update-material-classroom.js",
    "check:database": "npm run build && node dist/scripts/check-database.js",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:status": "prisma migrate status"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "@supabase/server": "^1.4.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.22.0",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.1.0",
    "prisma": "^7.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
