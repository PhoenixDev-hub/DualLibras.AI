# Configure these in Render's Environment panel; do not commit real values.
DATABASE_URL=
DIRECT_URL=
JWT_SECRET=
AI_INTERNAL_TOKEN=
ASSEMBLYAI_API_KEY=
CORS_ORIGIN=https://duallibrasai.vercel.app
COOKIE_SECURE=true
NODE_ENV=production
# PORT is supplied by Render. The image configures the internal service URLs.
